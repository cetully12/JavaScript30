// Adjust border color around photo when Spacing metric adjusted

// Blur photo when Blur metric adjusted

// Update border color when Base Color changed

const inputs = document.querySelectorAll('.controls input');